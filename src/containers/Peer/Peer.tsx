import React, { FunctionComponent } from 'react';
import DefaultLayout from '@/components/DefaultLayout';

const Peer = (props) => {
  return <DefaultLayout>Peer</DefaultLayout>;
};

export default Peer;
